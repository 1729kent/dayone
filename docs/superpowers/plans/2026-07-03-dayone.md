# DayOne Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 毎朝AIの「新入社員」がサンドボックスでリポジトリにオンボーディングし、腐ったセットアップ手順を検知→自己修復→ドキュメント修正PRを出す DayOne を Cloud Run 上に構築し、7/10 のハッカソン提出に間に合わせる。

**Architecture:** モノレポ1つ（`dayone`）に2つのデプロイ物: `dayone-app`（Cloud Run サービス、FastAPI ダッシュボード+トリガー API）と `dayone-rookie`（Cloud Run Jobs、エージェントパイプライン）。rookie は Job コンテナ内で完結し、進捗イベントを Firestore に逐次書き込み、app が SSE で中継する。LLM は Vertex AI 経由 Gemini（google-genai SDK）。パイプラインは決定的な制御フロー（Planner→Executor→Diagnostician→Reporter）で、LLM 呼び出しは各コンポーネント内に隔離しテストではフェイクに差し替える。

**Tech Stack:** Python 3.12 / uv / FastAPI / google-genai（Vertex AI, location=global）/ google-cloud-firestore / google-cloud-run / httpx / pytest / Docker（linux/amd64）/ GitHub Actions（WIF）

## Global Constraints

- 提出締切 2026-07-10 23:59。Task 1 完了時点で公開 URL が存在すること（初日デプロイ）
- Python 3.12、パッケージ管理は uv。lint は ruff
- LLM モデル: `gemini-3.5-flash`（主）、`gemini-3.1-flash-lite`（軽処理）。Vertex AI 経由（`genai.Client(vertexai=True, project=…, location="global")`）
- GCP: プロジェクト `dayone-hack-2026`（ID 重複時は末尾に数字を足す）、リージョン `asia-northeast1`、課金アカウント `019D7A-DE4B13-2C4E16`
- 暴走封じ込め: MAX_STEPS=30、MAX_ACTIONS=60（修復試行込み）、per-step timeout 120s、Job 全体 600s
- サンドボックスの子プロセスには**スクラブ済み環境変数のみ**渡す（`GITHUB_*` / `GOOGLE_*` / `DAYONE_*` / `*_TOKEN` / `*_KEY` を除去）
- GitHub PAT は fine-grained・デモリポジトリ限定・Secret Manager 経由で env 注入。コードにハードコードしない
- コミットは各タスク末尾で必ず行う。メッセージ末尾に Co-Authored-By: Claude Fable 5 <noreply@anthropic.com> を付ける
- すべての公開エンドポイントで秘密情報を返さない。POST /runs はクールダウン 300 秒

## File Structure

```
dayone/                          ← 本リポジトリ（公開）
├── pyproject.toml               ← 単一パッケージ・console_scripts 2つ
├── src/dayone/
│   ├── common/
│   │   ├── config.py            ← Settings（環境変数読み込み）
│   │   ├── models.py            ← 全 Pydantic モデル（Step, ExecResult, Run, …）
│   │   ├── store.py             ← Store プロトコル + MemoryStore + FirestoreStore
│   │   └── llm.py               ← LLM ラッパ（gen_json / gen_text、リトライ）
│   ├── rookie/
│   │   ├── sandbox.py           ← シェル実行 + 環境変数スクラブ
│   │   ├── planner.py           ← ドキュメント→StepPlan
│   │   ├── judge.py             ← OutputJudge（exit 0 でも怪しい出力の検知）
│   │   ├── executor.py          ← ステップ実行ループ（予算・ループ検知）
│   │   ├── diagnostician.py     ← 失敗診断・修復探索（function-calling ループ）
│   │   ├── scoring.py           ← 腐敗スコア・TTFS（純関数）
│   │   ├── github_client.py     ← ブランチ作成→ファイル更新→PR 作成
│   │   ├── reporter.py          ← 摩擦抽出・ドキュメント修正・PR・サマリ
│   │   └── main.py              ← Job エントリポイント（clone→pipeline→報告）
│   └── app/
│       ├── launcher.py          ← JobLauncher プロトコル + CloudRunJobLauncher
│       ├── server.py            ← FastAPI（/, /runs, SSE, /internal/scheduled）
│       └── static/index.html    ← ダッシュボード（ビルドレス）
├── tests/                       ← 上記と同構造の test_*.py
├── Dockerfile.app / Dockerfile.rookie
├── scripts/setup_gcp.sh         ← API 有効化・AR・Firestore・SA・Secret
├── .github/workflows/deploy.yml / e2e.yml
└── docs/                        ← 設計書・本プラン（既存）
```

別リポジトリ（Task 9 で作成、各 GitHub 公開リポ）: `dayone-demo-node`, `dayone-demo-py`（腐敗注入 `inject-rot.sh` / 修復 `heal.sh` 付き）

---

### Task 1: GCP プロジェクト + スキャフォールド + 初回デプロイ（今日中に URL 確保）

**Files:**
- Create: `pyproject.toml`, `src/dayone/__init__.py`, `src/dayone/common/__init__.py`, `src/dayone/common/config.py`, `src/dayone/app/__init__.py`, `src/dayone/app/server.py`（skeleton）, `Dockerfile.app`, `scripts/setup_gcp.sh`, `.gitignore`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Settings`（`from dayone.common.config import settings`）— fields: `project_id: str`, `region: str = "asia-northeast1"`, `genai_location: str = "global"`, `model: str = "gemini-3.5-flash"`, `model_lite: str = "gemini-3.1-flash-lite"`, `github_token: str = ""`, `job_name: str = "dayone-rookie"`, `sched_token: str = ""`, `create_pr: bool = True`。すべて環境変数 `DAYONE_*` で上書き可
- Produces: `create_app() -> FastAPI`（`dayone.app.server`）

- [ ] **Step 1: GCP プロジェクト作成と API 有効化スクリプトを書いて実行**

```bash
# scripts/setup_gcp.sh
#!/usr/bin/env bash
set -euo pipefail
PROJECT_ID="${PROJECT_ID:-dayone-hack-2026}"
REGION="asia-northeast1"
BILLING="019D7A-DE4B13-2C4E16"

gcloud projects create "$PROJECT_ID" 2>/dev/null || echo "project exists"
gcloud billing projects link "$PROJECT_ID" --billing-account="$BILLING"
gcloud config set project "$PROJECT_ID"
gcloud services enable run.googleapis.com cloudbuild.googleapis.com \
  artifactregistry.googleapis.com aiplatform.googleapis.com \
  firestore.googleapis.com cloudscheduler.googleapis.com \
  secretmanager.googleapis.com iamcredentials.googleapis.com
gcloud artifacts repositories create dayone --repository-format=docker \
  --location="$REGION" 2>/dev/null || echo "AR exists"
gcloud firestore databases create --location="$REGION" 2>/dev/null || echo "firestore exists"

# rookie 用の最小権限 SA
gcloud iam service-accounts create dayone-rookie 2>/dev/null || true
for role in roles/aiplatform.user roles/datastore.user; do
  gcloud projects add-iam-policy-binding "$PROJECT_ID" \
    --member="serviceAccount:dayone-rookie@${PROJECT_ID}.iam.gserviceaccount.com" \
    --role="$role" --condition=None -q
done
# app 用 SA（Job 起動 + Firestore）
gcloud iam service-accounts create dayone-app 2>/dev/null || true
for role in roles/run.developer roles/datastore.user roles/iam.serviceAccountUser; do
  gcloud projects add-iam-policy-binding "$PROJECT_ID" \
    --member="serviceAccount:dayone-app@${PROJECT_ID}.iam.gserviceaccount.com" \
    --role="$role" --condition=None -q
done
echo "OK: $PROJECT_ID"
```

Run: `bash scripts/setup_gcp.sh`
Expected: 最後に `OK: dayone-hack-2026`（ID 重複エラー時は `PROJECT_ID=dayone-hack-2026-2 bash scripts/setup_gcp.sh` で再実行し、以後全タスクでその ID を使う）

- [ ] **Step 2: uv プロジェクト初期化**

```bash
uv init --name dayone --package --python 3.12
uv add fastapi 'uvicorn[standard]' pydantic httpx google-genai google-cloud-firestore google-cloud-run
uv add --dev pytest ruff
```

`pyproject.toml` に追記:

```toml
[project.scripts]
dayone-app = "dayone.app.server:main"
dayone-rookie = "dayone.rookie.main:main"
```

- [ ] **Step 3: 失敗するテストを書く（Settings の環境変数上書き）**

```python
# tests/test_config.py
import os
from dayone.common.config import Settings

def test_settings_defaults():
    s = Settings(environ={})
    assert s.region == "asia-northeast1"
    assert s.model == "gemini-3.5-flash"
    assert s.create_pr is True

def test_settings_env_override():
    s = Settings(environ={"DAYONE_PROJECT_ID": "p1", "DAYONE_CREATE_PR": "0"})
    assert s.project_id == "p1"
    assert s.create_pr is False
```

Run: `uv run pytest tests/test_config.py -v` → Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 4: Settings 実装**

```python
# src/dayone/common/config.py
import os
from dataclasses import dataclass, field

@dataclass
class Settings:
    environ: dict = field(default_factory=lambda: dict(os.environ))

    def _get(self, key: str, default: str = "") -> str:
        return self.environ.get(f"DAYONE_{key}", default)

    @property
    def project_id(self) -> str: return self._get("PROJECT_ID")
    @property
    def region(self) -> str: return self._get("REGION", "asia-northeast1")
    @property
    def genai_location(self) -> str: return self._get("GENAI_LOCATION", "global")
    @property
    def model(self) -> str: return self._get("MODEL", "gemini-3.5-flash")
    @property
    def model_lite(self) -> str: return self._get("MODEL_LITE", "gemini-3.1-flash-lite")
    @property
    def github_token(self) -> str: return self._get("GITHUB_TOKEN")
    @property
    def job_name(self) -> str: return self._get("JOB_NAME", "dayone-rookie")
    @property
    def sched_token(self) -> str: return self._get("SCHED_TOKEN")
    @property
    def create_pr(self) -> bool: return self._get("CREATE_PR", "1") not in ("0", "false", "")

settings = Settings()
```

Run: `uv run pytest tests/test_config.py -v` → Expected: PASS

- [ ] **Step 5: skeleton FastAPI + Dockerfile.app**

```python
# src/dayone/app/server.py
import uvicorn
from fastapi import FastAPI

def create_app() -> FastAPI:
    app = FastAPI(title="DayOne")

    @app.get("/healthz")
    def healthz():
        return {"ok": True}

    @app.get("/")
    def index():
        return {"service": "DayOne", "status": "under construction"}

    return app

def main():
    import os
    uvicorn.run(create_app(), host="0.0.0.0", port=int(os.environ.get("PORT", "8080")))
```

```dockerfile
# Dockerfile.app
FROM python:3.12-slim
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv
WORKDIR /srv
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-install-project --no-dev
COPY src ./src
RUN uv sync --frozen --no-dev
ENV PATH="/srv/.venv/bin:$PATH"
CMD ["dayone-app"]
```

- [ ] **Step 6: 初回デプロイして URL 確保**

```bash
REGION=asia-northeast1; PROJECT_ID=$(gcloud config get-value project)
AR="$REGION-docker.pkg.dev/$PROJECT_ID/dayone"
gcloud auth configure-docker "$REGION-docker.pkg.dev" -q
docker buildx build --platform linux/amd64 -f Dockerfile.app -t "$AR/app:dev" --push .
gcloud run deploy dayone-app --image "$AR/app:dev" --region "$REGION" \
  --service-account "dayone-app@${PROJECT_ID}.iam.gserviceaccount.com" \
  --allow-unauthenticated --set-env-vars "DAYONE_PROJECT_ID=$PROJECT_ID"
curl -s "$(gcloud run services describe dayone-app --region $REGION --format='value(status.url)')/healthz"
```

Expected: `{"ok":true}` — **この URL が提出用 URL になる**

- [ ] **Step 7: GitHub 公開リポジトリ作成 + push + コミット**

```bash
git add -A && git commit -m "feat: scaffold + GCP setup + skeleton deploy"
gh repo create dayone --public --source . --push
```

---

### Task 2: モデルとストア（MemoryStore / FirestoreStore）

**Files:**
- Create: `src/dayone/common/models.py`, `src/dayone/common/store.py`
- Test: `tests/test_models.py`, `tests/test_store.py`

**Interfaces:**
- Produces（全タスクが consume する中核型・`dayone.common.models`）:

```python
ExecResult(exit_code: int, stdout: str, stderr: str, duration_s: float, timed_out: bool = False)
Step(id: int, intent: str, command: str, expects: str, source_doc_line: int | None = None)
StepPlan(doc_path: str, steps: list[Step])
Attempt(command: str, result: ExecResult)
Fix(category: Literal["doc_outdated","missing_prereq","code_bug"], working_command: str | None,
    explanation: str, doc_patch_hint: str)
StepOutcome(step: Step, status: Literal["ok","fixed","failed","skipped"],
    attempts: list[Attempt] = [], fix: Fix | None = None, suspicious: bool = False)
Friction(step_id: int, category: str, severity: Literal["low","medium","high"],
    doc_line: int | None, explanation: str, found_fix: bool)
RunReport(frictions: list[Friction], decay_score: int, ttfs_seconds: float | None,
    pr_url: str | None, summary: str)
Run(id: str, repo_url: str, trigger: Literal["manual","scheduled"], status: Literal["queued","running","completed","failed"],
    started_at: float, finished_at: float | None = None, decay_score: int | None = None,
    ttfs_seconds: float | None = None, pr_url: str | None = None, summary: str = "")
Event(seq: int, ts: float, type: str, payload: dict)
```

- Produces（`dayone.common.store`）:

```python
class Store(Protocol):
    def create_run(self, run: Run) -> None
    def update_run(self, run_id: str, **fields) -> None
    def get_run(self, run_id: str) -> Run | None
    def list_runs(self, limit: int = 20) -> list[Run]        # started_at 降順
    def append_event(self, run_id: str, type: str, payload: dict) -> int  # 採番した seq を返す
    def get_events(self, run_id: str, after_seq: int = -1) -> list[Event]
    def try_acquire_cooldown(self, seconds: int, now: float) -> bool
MemoryStore()          # テスト・ローカル用
FirestoreStore(project_id: str)  # runs/{id}, runs/{id}/events/{seq}, locks/trigger
```

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_store.py
from dayone.common.models import Run, Event
from dayone.common.store import MemoryStore

def make_run(rid="r1", ts=100.0):
    return Run(id=rid, repo_url="https://github.com/x/y", trigger="manual",
               status="queued", started_at=ts)

def test_run_crud():
    s = MemoryStore()
    s.create_run(make_run())
    s.update_run("r1", status="running")
    assert s.get_run("r1").status == "running"
    assert s.get_run("nope") is None

def test_list_runs_desc():
    s = MemoryStore()
    s.create_run(make_run("a", 1.0)); s.create_run(make_run("b", 2.0))
    assert [r.id for r in s.list_runs()] == ["b", "a"]

def test_events_seq_and_after():
    s = MemoryStore(); s.create_run(make_run())
    assert s.append_event("r1", "plan", {"n": 1}) == 0
    assert s.append_event("r1", "exec", {"n": 2}) == 1
    evs = s.get_events("r1", after_seq=0)
    assert len(evs) == 1 and evs[0].type == "exec"

def test_cooldown():
    s = MemoryStore()
    assert s.try_acquire_cooldown(300, now=1000.0) is True
    assert s.try_acquire_cooldown(300, now=1100.0) is False
    assert s.try_acquire_cooldown(300, now=1301.0) is True
```

Run: `uv run pytest tests/test_store.py -v` → Expected: FAIL (ImportError)

- [ ] **Step 2: models.py 実装（上記 Interfaces のシグネチャ通り、全て `pydantic.BaseModel`、`Literal` は `typing` から）**

- [ ] **Step 3: store.py 実装**

```python
# src/dayone/common/store.py
import time
from typing import Protocol
from dayone.common.models import Run, Event

class Store(Protocol):
    def create_run(self, run: Run) -> None: ...
    def update_run(self, run_id: str, **fields) -> None: ...
    def get_run(self, run_id: str) -> Run | None: ...
    def list_runs(self, limit: int = 20) -> list[Run]: ...
    def append_event(self, run_id: str, type: str, payload: dict) -> int: ...
    def get_events(self, run_id: str, after_seq: int = -1) -> list[Event]: ...
    def try_acquire_cooldown(self, seconds: int, now: float) -> bool: ...

class MemoryStore:
    def __init__(self):
        self._runs: dict[str, Run] = {}
        self._events: dict[str, list[Event]] = {}
        self._cooldown_until = 0.0

    def create_run(self, run: Run) -> None:
        self._runs[run.id] = run; self._events[run.id] = []

    def update_run(self, run_id: str, **fields) -> None:
        self._runs[run_id] = self._runs[run_id].model_copy(update=fields)

    def get_run(self, run_id: str) -> Run | None:
        return self._runs.get(run_id)

    def list_runs(self, limit: int = 20) -> list[Run]:
        return sorted(self._runs.values(), key=lambda r: -r.started_at)[:limit]

    def append_event(self, run_id: str, type: str, payload: dict) -> int:
        seq = len(self._events[run_id])
        self._events[run_id].append(Event(seq=seq, ts=time.time(), type=type, payload=payload))
        return seq

    def get_events(self, run_id: str, after_seq: int = -1) -> list[Event]:
        return [e for e in self._events.get(run_id, []) if e.seq > after_seq]

    def try_acquire_cooldown(self, seconds: int, now: float) -> bool:
        if now < self._cooldown_until:
            return False
        self._cooldown_until = now + seconds
        return True
```

FirestoreStore は同じインターフェースで実装する（`google.cloud.firestore.Client(project=project_id)`、runs コレクション、events はサブコレクション、seq はドキュメント `runs/{id}` の `event_count` を `firestore.Increment(1)` 付きトランザクションで採番、cooldown は `locks/trigger` ドキュメントの `until` フィールドをトランザクションで compare-and-set）。FirestoreStore の自動テストは書かない（Task 10 のライブ E2E で検証）。

- [ ] **Step 4: テスト通過を確認** — Run: `uv run pytest tests/ -v` → Expected: 全 PASS

- [ ] **Step 5: コミット** — `git add -A && git commit -m "feat: core models and store"`

---

### Task 3: サンドボックス（環境変数スクラブ + シェル実行）

**Files:**
- Create: `src/dayone/rookie/__init__.py`, `src/dayone/rookie/sandbox.py`
- Test: `tests/test_sandbox.py`

**Interfaces:**
- Consumes: `ExecResult`（Task 2）
- Produces（`dayone.rookie.sandbox`）:

```python
scrub_env(base: dict[str, str]) -> dict[str, str]   # 許可リスト方式 + 危険キー除去
class Sandbox:
    def __init__(self, cwd: Path, timeout_s: int = 120)
    def run(self, command: str, timeout_s: int | None = None) -> ExecResult  # shell=True, スクラブ済み env
```

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_sandbox.py
from pathlib import Path
from dayone.rookie.sandbox import Sandbox, scrub_env

def test_scrub_removes_secrets():
    env = {"PATH": "/bin", "HOME": "/h", "GITHUB_TOKEN": "x", "DAYONE_GITHUB_TOKEN": "x",
           "GOOGLE_APPLICATION_CREDENTIALS": "x", "MY_API_KEY": "x", "AWS_SECRET_TOKEN": "x",
           "LANG": "C", "NODE_ENV": "dev"}
    out = scrub_env(env)
    assert set(out) == {"PATH", "HOME", "LANG", "NODE_ENV"}

def test_run_captures_output(tmp_path: Path):
    r = Sandbox(cwd=tmp_path).run("echo hello && echo bad >&2 && exit 3")
    assert r.exit_code == 3 and "hello" in r.stdout and "bad" in r.stderr

def test_run_timeout(tmp_path: Path):
    r = Sandbox(cwd=tmp_path).run("sleep 5", timeout_s=1)
    assert r.timed_out and r.exit_code != 0

def test_child_env_is_scrubbed(tmp_path: Path, monkeypatch):
    monkeypatch.setenv("GITHUB_TOKEN", "secret")
    r = Sandbox(cwd=tmp_path).run("printenv GITHUB_TOKEN || echo EMPTY")
    assert "secret" not in r.stdout and "EMPTY" in r.stdout
```

Run: `uv run pytest tests/test_sandbox.py -v` → Expected: FAIL

- [ ] **Step 2: 実装**

```python
# src/dayone/rookie/sandbox.py
import subprocess, time
from pathlib import Path
from dayone.common.models import ExecResult

ALLOWED = {"PATH", "HOME", "LANG", "LC_ALL", "TERM", "TMPDIR", "USER", "SHELL",
           "NODE_ENV", "PYTHONUNBUFFERED", "npm_config_cache", "UV_CACHE_DIR"}
DENY_SUBSTR = ("TOKEN", "KEY", "SECRET", "CREDENTIAL", "PASSWORD")

def scrub_env(base: dict[str, str]) -> dict[str, str]:
    out = {}
    for k, v in base.items():
        if k not in ALLOWED:
            continue
        if any(s in k.upper() for s in DENY_SUBSTR):
            continue
        out[k] = v
    return out

class Sandbox:
    def __init__(self, cwd: Path, timeout_s: int = 120):
        self.cwd = cwd
        self.timeout_s = timeout_s

    def run(self, command: str, timeout_s: int | None = None) -> ExecResult:
        import os
        t0 = time.monotonic()
        try:
            p = subprocess.run(command, shell=True, cwd=self.cwd, env=scrub_env(dict(os.environ)),
                               capture_output=True, text=True, timeout=timeout_s or self.timeout_s)
            return ExecResult(exit_code=p.returncode, stdout=p.stdout[-20000:], stderr=p.stderr[-20000:],
                              duration_s=time.monotonic() - t0)
        except subprocess.TimeoutExpired as e:
            return ExecResult(exit_code=124, stdout=(e.stdout or "")[-20000:] if isinstance(e.stdout, str) else "",
                              stderr="timed out", duration_s=time.monotonic() - t0, timed_out=True)
```

- [ ] **Step 3: テスト通過確認** — Run: `uv run pytest tests/test_sandbox.py -v` → PASS
- [ ] **Step 4: コミット** — `git commit -am "feat: sandbox with env scrubbing"`

---

### Task 4: LLM ラッパ（gen_json / gen_text、リトライ）

**Files:**
- Create: `src/dayone/common/llm.py`
- Test: `tests/test_llm.py`

**Interfaces:**
- Consumes: `settings`（Task 1）
- Produces（`dayone.common.llm`）:

```python
class LLMError(Exception)
class LLM:
    def __init__(self, model: str, client=None)   # client=None なら genai.Client(vertexai=True, …) を遅延生成
    def gen_json(self, prompt: str, schema: type[BaseModel], system: str | None = None) -> BaseModel
    def gen_text(self, prompt: str, system: str | None = None) -> str
    def chat_with_tools(self, system: str, user: str, tools: list, on_call) -> str
        # function-calling ループ: モデルが tool を呼ぶ限り on_call(name, args)->str を実行して返す。最大12回
class FakeLLM:   # テスト用: 応答を enqueue するだけ
    def __init__(self, json_responses: list[BaseModel] = ..., text_responses: list[str] = ...)
```

- [ ] **Step 1: 失敗するテストを書く（リトライとフェイク）**

```python
# tests/test_llm.py
import pytest
from pydantic import BaseModel
from dayone.common.llm import LLM, FakeLLM, LLMError

class Out(BaseModel):
    x: int

class Flaky:
    """genai client の代役。2回 429 相当で失敗して3回目に成功する"""
    def __init__(self):
        self.calls = 0
        class Models:
            def __init__(s, outer): s.outer = outer
            def generate_content(s, **kw):
                s.outer.calls += 1
                if s.outer.calls < 3:
                    raise RuntimeError("429 RESOURCE_EXHAUSTED")
                class R: text = '{"x": 7}'
                return R()
        self.models = Models(self)

def test_gen_json_retries_then_parses():
    llm = LLM(model="m", client=Flaky())
    out = llm.gen_json("p", Out)
    assert out.x == 7

def test_gen_json_raises_after_3():
    class AlwaysFail(Flaky):
        def __init__(self):
            super().__init__()
            self.models.generate_content = lambda **kw: (_ for _ in ()).throw(RuntimeError("429"))
    with pytest.raises(LLMError):
        LLM(model="m", client=AlwaysFail()).gen_json("p", Out)

def test_fake_llm():
    f = FakeLLM(json_responses=[Out(x=1)], text_responses=["hi"])
    assert f.gen_json("p", Out).x == 1
    assert f.gen_text("p") == "hi"
```

Run: `uv run pytest tests/test_llm.py -v` → Expected: FAIL

- [ ] **Step 2: 実装（バックオフは 1s→2s→4s、`time.sleep` を `self._sleep` に切り出してテストではモンキーパッチ不要の高速化のため 0 秒に上書き可能にする。genai 呼び出しは `client.models.generate_content(model=…, contents=…, config=types.GenerateContentConfig(response_mime_type="application/json", response_schema=schema, system_instruction=system))`。`gen_json` は `schema.model_validate_json(resp.text)` で検証。`chat_with_tools` は `types.FunctionDeclaration` を渡し、`function_call` パートがある限り `on_call` を実行して `function_response` を返送、12 回で打ち切り最後のテキストを返す）**

- [ ] **Step 3: テスト通過確認** — `uv run pytest tests/test_llm.py -v` → PASS
- [ ] **Step 4: ライブ疎通スモーク（1回だけ・課金確認を兼ねる）**

```bash
DAYONE_PROJECT_ID=$(gcloud config get-value project) uv run python -c "
from dayone.common.llm import LLM
from dayone.common.config import settings
print(LLM(settings.model).gen_text('1+1=? 数字のみ'))"
```

Expected: `2`（Vertex AI 権限エラーが出たら `gcloud auth application-default login` を実行）

- [ ] **Step 5: コミット** — `git commit -am "feat: LLM wrapper with retry and fakes"`

---

### Task 5: Planner（ドキュメント → StepPlan）

**Files:**
- Create: `src/dayone/rookie/planner.py`, `src/dayone/rookie/prompts.py`
- Test: `tests/test_planner.py`

**Interfaces:**
- Consumes: `LLM.gen_json`（Task 4）、`StepPlan/Step`（Task 2）
- Produces（`dayone.rookie.planner`）:

```python
find_setup_doc(repo_dir: Path) -> Path            # README.md → CONTRIBUTING.md → docs/setup.md の優先順。無ければ FileNotFoundError
class Planner:
    def __init__(self, llm)                        # llm: LLM | FakeLLM
    def plan(self, repo_dir: Path) -> StepPlan     # doc を読み ≤15000 字に切って LLM でステップ化。steps は id 連番に正規化
```

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_planner.py
import pytest
from pathlib import Path
from dayone.common.models import Step, StepPlan
from dayone.common.llm import FakeLLM
from dayone.rookie.planner import Planner, find_setup_doc

def repo(tmp_path, files: dict[str, str]) -> Path:
    for name, body in files.items():
        p = tmp_path / name; p.parent.mkdir(parents=True, exist_ok=True); p.write_text(body)
    return tmp_path

def test_find_doc_priority(tmp_path):
    d = repo(tmp_path, {"CONTRIBUTING.md": "c", "README.md": "r"})
    assert find_setup_doc(d).name == "README.md"

def test_find_doc_missing(tmp_path):
    with pytest.raises(FileNotFoundError):
        find_setup_doc(tmp_path)

def test_plan_normalizes_ids(tmp_path):
    d = repo(tmp_path, {"README.md": "# setup\nnpm install\nnpm run setup"})
    fake_plan = StepPlan(doc_path="README.md", steps=[
        Step(id=7, intent="install deps", command="npm install", expects="node_modules created"),
        Step(id=9, intent="run setup", command="npm run setup", expects="setup done"),
    ])
    p = Planner(FakeLLM(json_responses=[fake_plan])).plan(d)
    assert [s.id for s in p.steps] == [1, 2]
    assert p.doc_path == "README.md"
```

Run: `uv run pytest tests/test_planner.py -v` → Expected: FAIL

- [ ] **Step 2: プロンプトを prompts.py に定数として実装**

```python
# src/dayone/rookie/prompts.py（抜粋・全文をこの内容で作成）
PLANNER_SYSTEM = """あなたは今日入社した新人エンジニア。渡されたセットアップドキュメントを、
上から順に実行する具体的なシェルコマンドのステップ列に変換する。
- ドキュメントに書いてあるコマンドだけを使う。書いていない手順を発明しない
- 1ステップ=1コマンド。cd が必要なら `cd x && cmd` の形で1つにまとめる
- expects には成功時に観測できる状態を1文で書く
- source_doc_line にはそのコマンドが書かれている行番号を入れる
- インタラクティブなコマンド（エディタ起動等）と、`rm -rf /` のような破壊的コマンドはスキップする"""

PLANNER_USER = """リポジトリのセットアップドキュメント（{doc_path}、行番号付き）:
---
{doc_body}
---
このドキュメント通りに環境構築するステッププランを JSON で返せ。"""
```

- [ ] **Step 3: Planner 実装**

```python
# src/dayone/rookie/planner.py
from pathlib import Path
from dayone.common.models import StepPlan
from dayone.rookie.prompts import PLANNER_SYSTEM, PLANNER_USER

DOC_CANDIDATES = ["README.md", "CONTRIBUTING.md", "docs/setup.md", "docs/SETUP.md", "SETUP.md"]

def find_setup_doc(repo_dir: Path) -> Path:
    for c in DOC_CANDIDATES:
        p = repo_dir / c
        if p.is_file():
            return p
    raise FileNotFoundError(f"no setup doc in {repo_dir}")

class Planner:
    def __init__(self, llm):
        self.llm = llm

    def plan(self, repo_dir: Path) -> StepPlan:
        doc = find_setup_doc(repo_dir)
        lines = doc.read_text(errors="replace").splitlines()
        numbered = "\n".join(f"{i+1}: {l}" for i, l in enumerate(lines))[:15000]
        rel = str(doc.relative_to(repo_dir))
        plan = self.llm.gen_json(
            PLANNER_USER.format(doc_path=rel, doc_body=numbered),
            StepPlan, system=PLANNER_SYSTEM)
        steps = [s.model_copy(update={"id": i + 1}) for i, s in enumerate(plan.steps)]
        return StepPlan(doc_path=rel, steps=steps)
```

- [ ] **Step 4: テスト通過確認** — `uv run pytest tests/test_planner.py -v` → PASS
- [ ] **Step 5: コミット** — `git commit -am "feat: planner converts docs to step plans"`

---

### Task 6: OutputJudge + Executor（実行ループ・予算・ループ検知）

**Files:**
- Create: `src/dayone/rookie/judge.py`, `src/dayone/rookie/executor.py`
- Test: `tests/test_executor.py`

**Interfaces:**
- Consumes: `Sandbox`（Task 3）、`Step/StepPlan/StepOutcome/Attempt/Fix`（Task 2）、Diagnostician は Protocol で受ける（Task 7 で実装）
- Produces（`dayone.rookie.executor`）:

```python
EmitFn = Callable[[str, dict], None]              # (event_type, payload)
class Diagnostician(Protocol):
    def diagnose(self, step: Step, attempts: list[Attempt], repo_dir: Path) -> Fix | None
class Budget:
    def __init__(self, max_steps: int = 30, max_actions: int = 60)
    def spend_action(self) -> bool                 # False = 予算切れ
class Executor:
    def __init__(self, sandbox, judge, diagnostician, emit: EmitFn, budget: Budget | None = None)
    def run(self, plan: StepPlan, repo_dir: Path) -> list[StepOutcome]
```
- Produces（`dayone.rookie.judge`）: `class OutputJudge: def __init__(self, llm=None); def suspicious(self, step, result) -> bool`（exit 0 のとき stderr/stdout に error/fatal/traceback/ENOENT があれば LLM lite に真偽確認、llm=None ならキーワードのみで判定）

**実行ポリシー（テストが仕様）:** 成功→`ok`／失敗→diagnose→`working_command` があり再実行成功→`fixed`／直らない→`failed`。`failed` が2連続したら残りは `skipped`。同一コマンドを通算3回実行したらループとみなし即打ち切り（残り `skipped`）。全ステップでアクション予算（実行回数）を消費。

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_executor.py
from pathlib import Path
from dayone.common.models import Step, StepPlan, Attempt, ExecResult, Fix
from dayone.rookie.executor import Executor, Budget
from dayone.rookie.judge import OutputJudge

def ok(cmd="c"): return ExecResult(exit_code=0, stdout="", stderr="", duration_s=0.1)
def ng(cmd="c"): return ExecResult(exit_code=1, stdout="", stderr="boom", duration_s=0.1)

class ScriptedSandbox:
    """コマンド→結果列の台本。同じコマンドが再度来たら列の次を返す"""
    def __init__(self, script: dict[str, list[ExecResult]]):
        self.script = {k: list(v) for k, v in script.items()}
        self.calls: list[str] = []
    def run(self, command, timeout_s=None):
        self.calls.append(command)
        seq = self.script.get(command, [ng()])
        return seq.pop(0) if len(seq) > 1 else seq[0]

class NoFix:
    def diagnose(self, step, attempts, repo_dir): return None

class FixWith:
    def __init__(self, cmd): self.cmd = cmd
    def diagnose(self, step, attempts, repo_dir):
        return Fix(category="doc_outdated", working_command=self.cmd,
                   explanation="renamed", doc_patch_hint="setup->bootstrap")

def plan(*cmds):
    return StepPlan(doc_path="README.md", steps=[
        Step(id=i+1, intent=f"s{i}", command=c, expects="ok") for i, c in enumerate(cmds)])

def run_exec(sandbox, diag, p):
    events = []
    ex = Executor(sandbox, OutputJudge(), diag, emit=lambda t, pl: events.append(t))
    return ex.run(p, Path(".")), events

def test_all_ok():
    outcomes, _ = run_exec(ScriptedSandbox({"a": [ok()], "b": [ok()]}), NoFix(), plan("a", "b"))
    assert [o.status for o in outcomes] == ["ok", "ok"]

def test_fix_verified_marks_fixed():
    sb = ScriptedSandbox({"npm run setup": [ng()], "npm run bootstrap": [ok()]})
    outcomes, _ = run_exec(sb, FixWith("npm run bootstrap"), plan("npm run setup"))
    assert outcomes[0].status == "fixed" and outcomes[0].fix.working_command == "npm run bootstrap"

def test_two_consecutive_failures_skip_rest():
    sb = ScriptedSandbox({"a": [ng()], "b": [ng()], "c": [ok()]})
    outcomes, _ = run_exec(sb, NoFix(), plan("a", "b", "c"))
    assert [o.status for o in outcomes] == ["failed", "failed", "skipped"]

def test_loop_detection_aborts():
    sb = ScriptedSandbox({"x": [ng(), ng(), ng(), ng()]})
    outcomes, _ = run_exec(sb, FixWith("x"), plan("x", "y"))  # 修復案も同じ x → ループ
    assert outcomes[-1].status == "skipped"
    assert sb.calls.count("x") <= 3

def test_action_budget():
    sb = ScriptedSandbox({f"c{i}": [ok()] for i in range(50)})
    ex = Executor(sb, OutputJudge(), NoFix(), emit=lambda t, p: None, budget=Budget(max_actions=5))
    outcomes = ex.run(plan(*[f"c{i}" for i in range(10)]), Path("."))
    assert sum(1 for o in outcomes if o.status == "ok") == 5
    assert all(o.status == "skipped" for o in outcomes[5:])

def test_judge_suspicious_triggers_diagnose():
    sus = ExecResult(exit_code=0, stdout="", stderr="Error: ENOENT missing file", duration_s=0.1)
    sb = ScriptedSandbox({"a": [sus], "a2": [ok()]})
    outcomes, _ = run_exec(sb, FixWith("a2"), plan("a"))
    assert outcomes[0].status == "fixed" and outcomes[0].suspicious is True
```

Run: `uv run pytest tests/test_executor.py -v` → Expected: FAIL

- [ ] **Step 2: judge.py 実装（キーワード: `error` `fatal` `traceback` `enoent` `not found` を小文字比較。llm があれば MODEL_LITE で「このコマンドは成功したか? yes/no」を確認し no のときだけ suspicious）**

- [ ] **Step 3: executor.py 実装**

```python
# src/dayone/rookie/executor.py
from pathlib import Path
from typing import Callable, Protocol
from dayone.common.models import Step, StepPlan, StepOutcome, Attempt, Fix

EmitFn = Callable[[str, dict], None]

class Diagnostician(Protocol):
    def diagnose(self, step: Step, attempts: list[Attempt], repo_dir: Path) -> Fix | None: ...

class Budget:
    def __init__(self, max_steps: int = 30, max_actions: int = 60):
        self.max_steps = max_steps
        self.actions = max_actions
    def spend_action(self) -> bool:
        if self.actions <= 0:
            return False
        self.actions -= 1
        return True

class Executor:
    def __init__(self, sandbox, judge, diagnostician, emit: EmitFn, budget: Budget | None = None):
        self.sandbox, self.judge, self.diag, self.emit = sandbox, judge, diagnostician, emit
        self.budget = budget or Budget()

    def run(self, plan: StepPlan, repo_dir: Path) -> list[StepOutcome]:
        outcomes: list[StepOutcome] = []
        cmd_counts: dict[str, int] = {}
        consecutive_failures = 0
        aborted = False

        def execute(cmd: str):
            cmd_counts[cmd] = cmd_counts.get(cmd, 0) + 1
            return self.sandbox.run(cmd)

        for step in plan.steps[: self.budget.max_steps]:
            if aborted or consecutive_failures >= 2 or not self.budget.spend_action():
                outcomes.append(StepOutcome(step=step, status="skipped"))
                continue
            if cmd_counts.get(step.command, 0) >= 3:
                aborted = True
                outcomes.append(StepOutcome(step=step, status="skipped"))
                continue
            self.emit("exec", {"step_id": step.id, "command": step.command, "intent": step.intent})
            result = execute(step.command)
            attempts = [Attempt(command=step.command, result=result)]
            suspicious = result.exit_code == 0 and self.judge.suspicious(step, result)
            if result.exit_code == 0 and not suspicious:
                consecutive_failures = 0
                outcomes.append(StepOutcome(step=step, status="ok", attempts=attempts))
                continue
            self.emit("diagnose", {"step_id": step.id, "stderr": result.stderr[-500:]})
            fix = self.diag.diagnose(step, attempts, repo_dir)
            fixed = False
            if fix and fix.working_command and self.budget.spend_action():
                if cmd_counts.get(fix.working_command, 0) >= 3:
                    aborted = True
                else:
                    verify = execute(fix.working_command)
                    attempts.append(Attempt(command=fix.working_command, result=verify))
                    fixed = verify.exit_code == 0
            if fixed:
                consecutive_failures = 0
                self.emit("fix", {"step_id": step.id, "working_command": fix.working_command,
                                  "category": fix.category})
                outcomes.append(StepOutcome(step=step, status="fixed", attempts=attempts,
                                            fix=fix, suspicious=suspicious))
            else:
                consecutive_failures += 1
                outcomes.append(StepOutcome(step=step, status="failed", attempts=attempts,
                                            fix=fix, suspicious=suspicious))
        return outcomes
```

- [ ] **Step 4: テスト通過確認** — `uv run pytest tests/test_executor.py -v` → PASS（`test_action_budget` の期待値が実装と食い違う場合、実装ではなくテストの意図＝「予算切れ後は skipped」を優先して調整する）
- [ ] **Step 5: コミット** — `git commit -am "feat: executor loop with budgets and loop detection"`

---

### Task 7: Diagnostician（診断・修復探索の function-calling ループ）

**Files:**
- Create: `src/dayone/rookie/diagnostician.py`
- Modify: `src/dayone/rookie/prompts.py`（DIAGNOSE_SYSTEM 追加）
- Test: `tests/test_diagnostician.py`

**Interfaces:**
- Consumes: `LLM.chat_with_tools`（Task 4）、`Sandbox`（Task 3）、`Fix/Step/Attempt`（Task 2）
- Produces（`dayone.rookie.diagnostician`）:

```python
class RepoTools:                                   # 探索ツール（すべて repo_dir 内に制限）
    def __init__(self, repo_dir: Path, sandbox: Sandbox, probe_budget: int = 5)
    def list_dir(self, path: str = ".") -> str
    def read_file(self, path: str, max_chars: int = 8000) -> str
    def search(self, pattern: str) -> str          # 全テキストファイルの部分一致 grep、上位20件
    def run_probe(self, command: str) -> str       # sandbox 実行（予算付き）。結果を文字列で返す
class GeminiDiagnostician:                          # Executor の Diagnostician Protocol を満たす
    def __init__(self, llm, sandbox_factory: Callable[[Path], Sandbox] = Sandbox)
    def diagnose(self, step: Step, attempts: list[Attempt], repo_dir: Path) -> Fix | None
```

診断の最終出力は LLM に `Fix` スキーマの JSON で返させる（`chat_with_tools` の最終テキストを `Fix.model_validate_json` でパース。失敗したら `gen_json` で整形リトライ1回、それでもだめなら None）。

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_diagnostician.py
import json
from pathlib import Path
from dayone.common.models import Step, Attempt, ExecResult, Fix
from dayone.rookie.diagnostician import RepoTools, GeminiDiagnostician
from dayone.rookie.sandbox import Sandbox

def test_repotools_confined(tmp_path):
    (tmp_path / "a.txt").write_text("hello npm run bootstrap world")
    t = RepoTools(tmp_path, Sandbox(cwd=tmp_path))
    assert "a.txt" in t.list_dir(".")
    assert "bootstrap" in t.read_file("a.txt")
    assert "a.txt" in t.search("bootstrap")
    assert "outside" in t.read_file("../../etc/passwd")  # 拒否メッセージを返す

def test_probe_budget(tmp_path):
    t = RepoTools(tmp_path, Sandbox(cwd=tmp_path), probe_budget=1)
    assert "exit_code=0" in t.run_probe("true")
    assert "budget" in t.run_probe("true")  # 2回目は拒否

class ToolUsingFakeLLM:
    """search を1回呼んでから Fix JSON を返す台本"""
    def chat_with_tools(self, system, user, tools, on_call):
        on_call("search", {"pattern": "bootstrap"})
        return json.dumps({"category": "doc_outdated", "working_command": "npm run bootstrap",
                           "explanation": "script renamed", "doc_patch_hint": "setup->bootstrap"})

def test_diagnose_returns_fix(tmp_path):
    (tmp_path / "package.json").write_text('{"scripts": {"bootstrap": "true"}}')
    d = GeminiDiagnostician(ToolUsingFakeLLM())
    fix = d.diagnose(Step(id=1, intent="setup", command="npm run setup", expects="done"),
                     [Attempt(command="npm run setup",
                              result=ExecResult(exit_code=1, stdout="", stderr="missing script: setup", duration_s=0.1))],
                     tmp_path)
    assert fix.category == "doc_outdated" and fix.working_command == "npm run bootstrap"

class GarbageLLM:
    def chat_with_tools(self, system, user, tools, on_call): return "no json here"
    def gen_json(self, prompt, schema, system=None): raise RuntimeError("nope")

def test_diagnose_garbage_returns_none(tmp_path):
    d = GeminiDiagnostician(GarbageLLM())
    fix = d.diagnose(Step(id=1, intent="x", command="c", expects="e"),
                     [Attempt(command="c", result=ExecResult(exit_code=1, stdout="", stderr="", duration_s=0.1))],
                     tmp_path)
    assert fix is None
```

Run: `uv run pytest tests/test_diagnostician.py -v` → Expected: FAIL

- [ ] **Step 2: RepoTools 実装（パスは `Path(repo_dir / path).resolve()` が `repo_dir.resolve()` 配下でなければ `"outside repository"` を返す。search は `rglob("*")` でテキストファイルのみ、1MB 超・バイナリはスキップ、`f"{path}:{line_no}: {line}"` 形式で上位20件。run_probe は予算超過で `"probe budget exceeded"`、実行結果は `f"exit_code={r.exit_code}\nstdout: {r.stdout[-2000:]}\nstderr: {r.stderr[-2000:]}"`）**

- [ ] **Step 3: DIAGNOSE_SYSTEM プロンプトを prompts.py に追加**

```python
DIAGNOSE_SYSTEM = """あなたはセットアップ手順の失敗を診断する新人エンジニア。ツールでリポジトリを探索し、
失敗原因を3分類のどれか判定する:
- doc_outdated: ドキュメントが古い（コマンド改名・ファイル移動など）。working_command に実際に動くはずのコマンドを入れる
- missing_prereq: 前提の記載漏れ（未記載の必須ツール・環境変数・事前手順）。working_command には前提を満たしてから実行するコマンド（`A && B` 形式可）を入れる
- code_bug: リポジトリのコード自体のバグ。working_command は null
手がかり: package.json / pyproject.toml / Makefile の実際のタスク名、類似ファイル名の search。
run_probe は高価なので仮説の確認にだけ使う（最大数回）。
最後に必ず Fix スキーマの JSON だけを出力する:
{"category": "...", "working_command": "... or null", "explanation": "...", "doc_patch_hint": "ドキュメントのどこをどう直すか1文"}"""
```

- [ ] **Step 4: GeminiDiagnostician 実装（tools は list_dir/read_file/search/run_probe の 4 つを FunctionDeclaration 化。user メッセージには step.intent / step.command / step.expects / attempts の exit_code と stderr 末尾 2000 字を含める。最終テキストから最初の `{` 〜最後の `}` を抜き出して `Fix.model_validate_json`、失敗時は gen_json で1回整形、例外は None）**

- [ ] **Step 5: テスト通過確認** — `uv run pytest tests/test_diagnostician.py -v` → PASS
- [ ] **Step 6: コミット** — `git commit -am "feat: diagnostician with repo exploration tools"`

---

### Task 8: scoring + GitHub PR クライアント + Reporter

**Files:**
- Create: `src/dayone/rookie/scoring.py`, `src/dayone/rookie/github_client.py`, `src/dayone/rookie/reporter.py`
- Modify: `src/dayone/rookie/prompts.py`（DOC_PATCH_SYSTEM, SUMMARY_SYSTEM 追加）
- Test: `tests/test_scoring.py`, `tests/test_reporter.py`

**Interfaces:**
- Consumes: `StepOutcome/Friction/RunReport`（Task 2）、`LLM`（Task 4）
- Produces:

```python
# dayone.rookie.scoring（純関数・LLM 不使用）
frictions_from(outcomes: list[StepOutcome]) -> list[Friction]
   # fixed→category は fix.category、severity="medium"（suspicious 起点なら "low"）、found_fix=True
   # failed→category は fix.category（fix なしなら "code_bug"）、severity="high"、found_fix=False
   # ok/skipped は摩擦なし
decay_score(frictions: list[Friction]) -> int      # low=5, medium=15, high=30 の合計、上限100
ttfs(outcomes, started_at: float, finished_at: float) -> float | None  # 全 steps が ok/fixed なら経過秒、それ以外 None

# dayone.rookie.github_client
class GitHubClient:
    def __init__(self, token: str, http=None)      # http: httpx.Client 互換（テストでフェイク差し込み）
    def create_doc_pr(self, repo_full: str, base: str, file_path: str,
                      new_content: str, title: str, body: str, branch: str) -> str  # PR の html_url
    # 実装: GET /repos/{r}/git/ref/heads/{base} → POST /repos/{r}/git/refs（ブランチ作成）
    #      → GET /repos/{r}/contents/{path}?ref={base}（現 sha 取得）
    #      → PUT /repos/{r}/contents/{path}（branch 指定で更新）→ POST /repos/{r}/pulls

# dayone.rookie.reporter
class Reporter:
    def __init__(self, llm, github: GitHubClient | None, emit: EmitFn, create_pr: bool = True)
    def report(self, repo_url: str, repo_dir: Path, plan: StepPlan,
               outcomes: list[StepOutcome], started_at: float, finished_at: float) -> RunReport
    # found_fix な摩擦が1件以上 & create_pr & github → doc を LLM で修正して PR、pr_url を格納
    # summary は SUMMARY_SYSTEM で 2文生成。PR 失敗は握りつぶさず emit("error") して pr_url=None で続行
```

- [ ] **Step 1: scoring の失敗するテストを書く**

```python
# tests/test_scoring.py
from dayone.common.models import Step, StepOutcome, Attempt, ExecResult, Fix, Friction
from dayone.rookie.scoring import frictions_from, decay_score, ttfs

def step(i): return Step(id=i, intent="x", command=f"c{i}", expects="e")
def att(code): return [Attempt(command="c", result=ExecResult(exit_code=code, stdout="", stderr="", duration_s=1))]
FIX = Fix(category="doc_outdated", working_command="c2", explanation="", doc_patch_hint="h")

def test_frictions_mapping():
    outcomes = [
        StepOutcome(step=step(1), status="ok", attempts=att(0)),
        StepOutcome(step=step(2), status="fixed", attempts=att(1), fix=FIX),
        StepOutcome(step=step(3), status="failed", attempts=att(1)),
        StepOutcome(step=step(4), status="skipped"),
    ]
    fr = frictions_from(outcomes)
    assert [(f.step_id, f.severity, f.found_fix) for f in fr] == [(2, "medium", True), (3, "high", False)]
    assert fr[1].category == "code_bug"

def test_decay_score_caps():
    fr = [Friction(step_id=i, category="doc_outdated", severity="high", doc_line=None,
                   explanation="", found_fix=False) for i in range(5)]
    assert decay_score(fr) == 100
    assert decay_score([]) == 0

def test_ttfs():
    ok_all = [StepOutcome(step=step(1), status="ok"), StepOutcome(step=step(2), status="fixed", fix=FIX)]
    assert ttfs(ok_all, 100.0, 250.0) == 150.0
    with_fail = ok_all + [StepOutcome(step=step(3), status="failed")]
    assert ttfs(with_fail, 100.0, 250.0) is None
```

Run: `uv run pytest tests/test_scoring.py -v` → FAIL → **Step 2: scoring.py 実装（Interfaces のロジック通り）** → PASS

- [ ] **Step 3: Reporter + GitHubClient の失敗するテストを書く**

```python
# tests/test_reporter.py
from pathlib import Path
from dayone.common.models import Step, StepPlan, StepOutcome, Attempt, ExecResult, Fix
from dayone.common.llm import FakeLLM
from dayone.rookie.reporter import Reporter
from dayone.rookie.github_client import GitHubClient

def test_create_doc_pr_calls_github_api():
    calls = []
    class FakeHTTP:
        def get(self, url, **kw):
            calls.append(("GET", url))
            class R:
                status_code = 200
                def json(s2): return {"object": {"sha": "abc"}, "sha": "filesha", "content": ""}
                def raise_for_status(s2): pass
            return R()
        def post(self, url, **kw):
            calls.append(("POST", url))
            class R:
                status_code = 201
                def json(s2): return {"html_url": "https://github.com/x/y/pull/1"}
                def raise_for_status(s2): pass
            return R()
        def put(self, url, **kw):
            calls.append(("PUT", url))
            class R:
                status_code = 200
                def json(s2): return {}
                def raise_for_status(s2): pass
            return R()
    gh = GitHubClient(token="t", http=FakeHTTP())
    url = gh.create_doc_pr("x/y", "main", "README.md", "new", "title", "body", "dayone/fix-1")
    assert url.endswith("/pull/1")
    assert ("POST", "https://api.github.com/repos/x/y/pulls") in calls

def test_reporter_generates_pr_and_summary(tmp_path):
    (tmp_path / "README.md").write_text("npm run setup")
    plan = StepPlan(doc_path="README.md", steps=[Step(id=1, intent="setup", command="npm run setup", expects="done")])
    outcomes = [StepOutcome(step=plan.steps[0], status="fixed",
        attempts=[Attempt(command="npm run setup", result=ExecResult(exit_code=1, stdout="", stderr="", duration_s=1))],
        fix=Fix(category="doc_outdated", working_command="npm run bootstrap", explanation="renamed", doc_patch_hint="h"))]
    class FakeGH:
        def create_doc_pr(self, *a, **kw): return "https://github.com/x/y/pull/9"
    llm = FakeLLM(text_responses=["npm run bootstrap", "サマリです。修正PRを出しました。"])
    rep = Reporter(llm, FakeGH(), emit=lambda t, p: None).report(
        "https://github.com/x/y", tmp_path, plan, outcomes, 0.0, 60.0)
    assert rep.pr_url == "https://github.com/x/y/pull/9"
    assert rep.decay_score == 15 and rep.ttfs_seconds == 60.0 and rep.summary

def test_reporter_no_frictions_no_pr(tmp_path):
    (tmp_path / "README.md").write_text("x")
    plan = StepPlan(doc_path="README.md", steps=[Step(id=1, intent="i", command="c", expects="e")])
    outcomes = [StepOutcome(step=plan.steps[0], status="ok")]
    called = []
    class FakeGH:
        def create_doc_pr(self, *a, **kw): called.append(1); return "u"
    rep = Reporter(FakeLLM(text_responses=["s"]), FakeGH(), emit=lambda t, p: None).report(
        "https://github.com/x/y", tmp_path, plan, outcomes, 0.0, 30.0)
    assert rep.pr_url is None and not called and rep.decay_score == 0
```

Run: `uv run pytest tests/test_reporter.py -v` → Expected: FAIL

- [ ] **Step 4: github_client.py / reporter.py / プロンプト実装**

DOC_PATCH_SYSTEM（prompts.py へ）:

```python
DOC_PATCH_SYSTEM = """あなたはドキュメント修正担当。元のセットアップドキュメント全文と、
「ドキュメントの記載コマンド → 実際に動いたコマンド」の対応表・診断メモを渡す。
最小限の修正を加えたドキュメント全文だけを出力する（コードフェンスで囲まない）。
記載のない手順の追加は missing_prereq の前提の明記のみ許可。文体は元のまま維持する。"""
SUMMARY_SYSTEM = "あなたは新人エンジェント DayOne。今日のオンボーディング結果を日本語2文で報告する。1文目に摩擦の要点、2文目に対応内容。"
```

Reporter.report の流れ: `frictions_from` → `decay_score` / `ttfs` → found_fix ありなら `repo_dir/plan.doc_path` を読み、対応表（`step.command` → `fix.working_command`、`fix.doc_patch_hint` 併記）とともに DOC_PATCH_SYSTEM で新全文を生成 → 変更があるときだけ `github.create_doc_pr(repo_full=repo_url から owner/name 抽出, base="main", file_path=plan.doc_path, branch=f"dayone/fix-{int(finished_at)}", title="docs: DayOneが検知したセットアップ手順の腐敗を修正", body=摩擦一覧のMarkdown)` → summary 生成 → RunReport 返却。

- [ ] **Step 5: テスト通過確認** — `uv run pytest tests/ -v` → 全 PASS
- [ ] **Step 6: コミット** — `git commit -am "feat: scoring, github PR client, reporter"`

---

### Task 9: デモリポジトリ 2 個（腐敗注入・修復スクリプト付き）

**Files:**（別リポジトリ、`/Users/kent/Uto/hackthon-demos/` 配下に作成して gh で公開）
- Create: `dayone-demo-node/`（`package.json`, `index.js`, `README.md`, `inject-rot.sh`, `heal.sh`）
- Create: `dayone-demo-py/`（`pyproject.toml`, `main.py`, `README.md`, `inject-rot.sh`, `heal.sh`）

**Interfaces:**
- Produces: 公開リポジトリ `1729kent/dayone-demo-node`（腐敗: `npm run setup`→実体は `bootstrap` に改名、`.env.example` のキー名変更）と `1729kent/dayone-demo-py`（腐敗: README から `uv run python -m db_init` の手順が欠落 → アプリ起動が失敗）
- 両リポジトリとも `main` ブランチ、依存最小（node: 依存ゼロの素の Express なし・標準 http のみ / py: 標準ライブラリのみ）で 1 実行 3 分以内

- [ ] **Step 1: dayone-demo-node を作る**

`package.json`:

```json
{
  "name": "dayone-demo-node",
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "node -e \"require('fs').copyFileSync('.env.example','.env');console.log('env ready')\"",
    "test": "node -e \"require('fs').accessSync('.env');console.log('ok')\"",
    "start": "node index.js"
  }
}
```

`index.js`: `require('http').createServer((q,s)=>s.end('demo')).listen(process.env.PORT||3000)`
`.env.example`: `APP_GREETING=hello`
`README.md`（腐敗後の姿。`npm run setup` は存在しない = doc_outdated）:

```markdown
# dayone-demo-node
## Setup
1. `npm install`
2. `npm run setup`     ← 実体は bootstrap に改名済み（腐敗ポイント）
3. `npm test`
```

`inject-rot.sh` は README を上記（setup 記載）に、`heal.sh` は `npm run bootstrap` 記載に書き換えるだけの `sed` スクリプト。

- [ ] **Step 2: dayone-demo-py を作る**

`main.py` は `data/db.json` が無ければ `SystemExit("run db_init first")`、`db_init.py` は `data/db.json` を作る。README の手順は `uv sync` → `uv run python main.py`（`uv run python db_init.py` の手順が欠落 = missing_prereq）。`heal.sh` は README に db_init 手順を挿入する。

- [ ] **Step 3: 公開して動作確認**

```bash
cd /Users/kent/Uto/hackthon-demos/dayone-demo-node && git init -b main && git add -A && git commit -m "demo repo" && gh repo create dayone-demo-node --public --source . --push
cd ../dayone-demo-py && git init -b main && git add -A && git commit -m "demo repo" && gh repo create dayone-demo-py --public --source . --push
```

手動確認: クリーンな一時ディレクトリで README 通りに実行し、node は step 2 で `missing script: setup`、py は起動時 `run db_init first` で**意図通り失敗する**こと。

- [ ] **Step 4: fine-grained PAT を作成して Secret Manager に登録（ユーザー操作）**

GitHub → Settings → Developer settings → Fine-grained tokens: リポジトリ `dayone-demo-node`, `dayone-demo-py` のみ、権限 Contents:RW + Pull requests:RW。

```bash
echo -n "<PAT>" | gcloud secrets create dayone-github-token --data-file=-
gcloud secrets add-iam-policy-binding dayone-github-token \
  --member="serviceAccount:dayone-rookie@$(gcloud config get-value project).iam.gserviceaccount.com" \
  --role=roles/secretmanager.secretAccessor
```

---

### Task 10: rookie main + Job コンテナ + ライブ E2E（コアデモ成立）

**Files:**
- Create: `src/dayone/rookie/main.py`, `Dockerfile.rookie`, `scripts/run_rookie_local.sh`
- Test: `tests/test_rookie_main.py`

**Interfaces:**
- Consumes: これまでの全コンポーネント + `FirestoreStore`
- Produces（`dayone.rookie.main`）:

```python
run_pipeline(repo_url: str, run_id: str, store: Store, work_dir: Path, settings) -> Run
   # clone → plan → execute → report → store 更新。例外時も status="failed" + それまでのイベントは保存済み
main() -> None   # env: DAYONE_RUN_ID, DAYONE_REPO_URL。FirestoreStore を組み立てて run_pipeline
```

- [ ] **Step 1: 失敗するテストを書く（オーケストレーションのみ、全部フェイク）**

```python
# tests/test_rookie_main.py
from pathlib import Path
from dayone.common.config import Settings
from dayone.common.models import Run, Step, StepPlan, StepOutcome
from dayone.common.store import MemoryStore
from dayone.rookie import main as rookie_main

def test_run_pipeline_happy(tmp_path, monkeypatch):
    store = MemoryStore()
    store.create_run(Run(id="r1", repo_url="u", trigger="manual", status="queued", started_at=0.0))
    plan = StepPlan(doc_path="README.md", steps=[Step(id=1, intent="i", command="true", expects="e")])
    monkeypatch.setattr(rookie_main, "clone_repo", lambda url, dst: (dst / "README.md").write_text("x"))
    monkeypatch.setattr(rookie_main, "build_planner", lambda s: type("P", (), {"plan": lambda self, d: plan})())
    monkeypatch.setattr(rookie_main, "build_diagnostician", lambda s: type("D", (), {"diagnose": lambda self, *a: None})())
    monkeypatch.setattr(rookie_main, "build_reporter", lambda s, emit: type("R", (), {"report": lambda self, *a, **k:
        __import__("dayone.common.models", fromlist=["RunReport"]).RunReport(
            frictions=[], decay_score=0, ttfs_seconds=1.0, pr_url=None, summary="ok")})())
    run = rookie_main.run_pipeline("u", "r1", store, tmp_path, Settings(environ={}))
    assert run.status == "completed" and store.get_run("r1").decay_score == 0
    types = [e.type for e in store.get_events("r1")]
    assert "plan" in types and "report" in types

def test_run_pipeline_failure_marks_failed(tmp_path, monkeypatch):
    store = MemoryStore()
    store.create_run(Run(id="r1", repo_url="u", trigger="manual", status="queued", started_at=0.0))
    monkeypatch.setattr(rookie_main, "clone_repo",
                        lambda url, dst: (_ for _ in ()).throw(RuntimeError("clone failed")))
    run = rookie_main.run_pipeline("u", "r1", store, tmp_path, Settings(environ={}))
    assert run.status == "failed"
    assert any(e.type == "error" for e in store.get_events("r1"))
```

Run: `uv run pytest tests/test_rookie_main.py -v` → Expected: FAIL

- [ ] **Step 2: main.py 実装（`clone_repo` は `git clone --depth 1`。`build_planner/build_diagnostician/build_reporter/build_judge` はモジュールレベル関数（テストで差し替えるため）。emit は `store.append_event(run_id, …)` + 標準出力ログの二重化。イベント: `phase`(clone/plan/exec/report), `plan`(steps 概要), `think`(LLM の説明文), `exec`/`diagnose`/`fix`（Executor から）, `report`(RunReport dump), `error`。run_pipeline 全体を try/except で包み、finally で `finished_at` を更新）**

- [ ] **Step 3: テスト通過確認** — `uv run pytest tests/ -v` → 全 PASS → コミット

- [ ] **Step 4: ローカルで本物の E2E（LLM は本物・store は MemoryStore→標準出力）**

```bash
# scripts/run_rookie_local.sh
DAYONE_PROJECT_ID=$(gcloud config get-value project) \
DAYONE_GITHUB_TOKEN=$(gcloud secrets versions access latest --secret dayone-github-token) \
DAYONE_RUN_ID=local-$(date +%s) DAYONE_REPO_URL=https://github.com/1729kent/dayone-demo-node \
DAYONE_STORE=memory uv run dayone-rookie
```

Expected: `npm run setup` 失敗 → diagnose → `npm run bootstrap` 発見 → fixed → **dayone-demo-node に PR が作られる**。github.com で PR の diff（README の setup→bootstrap）を目視確認。ここが**プロジェクト全体のコア検証ポイント**。動かなければ prompts を調整して再実行（Executor/Diagnostician のテストは触らない）。

- [ ] **Step 5: Dockerfile.rookie 作成とデプロイ**

```dockerfile
# Dockerfile.rookie
FROM python:3.12-slim
RUN apt-get update && apt-get install -y --no-install-recommends git curl nodejs npm \
 && rm -rf /var/lib/apt/lists/*
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv
WORKDIR /srv
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-install-project --no-dev
COPY src ./src
RUN uv sync --frozen --no-dev
ENV PATH="/srv/.venv/bin:$PATH" PYTHONUNBUFFERED=1
CMD ["dayone-rookie"]
```

```bash
REGION=asia-northeast1; PROJECT_ID=$(gcloud config get-value project)
AR="$REGION-docker.pkg.dev/$PROJECT_ID/dayone"
docker buildx build --platform linux/amd64 -f Dockerfile.rookie -t "$AR/rookie:dev" --push .
gcloud run jobs create dayone-rookie --image "$AR/rookie:dev" --region "$REGION" \
  --service-account "dayone-rookie@${PROJECT_ID}.iam.gserviceaccount.com" \
  --task-timeout 600 --memory 1Gi --max-retries 0 \
  --set-env-vars "DAYONE_PROJECT_ID=$PROJECT_ID" \
  --set-secrets "DAYONE_GITHUB_TOKEN=dayone-github-token:latest" 2>/dev/null || \
gcloud run jobs update dayone-rookie --image "$AR/rookie:dev" --region "$REGION"
# クラウド上で1回実行して Firestore にイベントが入ることを確認
gcloud run jobs execute dayone-rookie --region "$REGION" \
  --update-env-vars "DAYONE_RUN_ID=cloud-test-1,DAYONE_REPO_URL=https://github.com/1729kent/dayone-demo-node" --wait
```

Expected: 実行成功、`gcloud firestore` コンソールで `runs/cloud-test-1` と events サブコレクションを確認

- [ ] **Step 6: コミット** — `git commit -am "feat: rookie job end-to-end on Cloud Run"`

---

### Task 11: dayone-app API（トリガー・クールダウン・SSE・Scheduler 受け口）

**Files:**
- Create: `src/dayone/app/launcher.py`
- Modify: `src/dayone/app/server.py`（skeleton を置き換え）
- Test: `tests/test_server.py`

**Interfaces:**
- Consumes: `Store`（Task 2）
- Produces（`dayone.app.launcher`）:

```python
class JobLauncher(Protocol):
    def launch(self, run_id: str, repo_url: str) -> None
class CloudRunJobLauncher:   # google.cloud.run_v2.JobsClient().run_job(RunJobRequest(name=…, overrides=…))
    def __init__(self, project_id: str, region: str, job_name: str)
```

- Produces（server.py）: `create_app(store: Store | None = None, launcher: JobLauncher | None = None, settings=None) -> FastAPI`
  - `GET /healthz`、`GET /`（static/index.html）、`GET /api/runs`、`GET /api/runs/{id}`、`GET /api/runs/{id}/events?after=N`
  - `POST /runs` body `{repo_url?: str}`（省略時 demo-node）→ クールダウン NG なら 429 `{retry: true}`、OK なら run 作成+launch → `{run_id}`
  - `GET /api/runs/{id}/stream`（SSE: 1 秒ポーリングで新イベントを `data:` 送出、status が completed/failed になったら `event: done` を送って終了）
  - `POST /internal/scheduled?token=…` → `settings.sched_token` と一致しなければ 403、一致すればクールダウン無視で demo リポジトリ 2 つの run を起動

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_server.py
from fastapi.testclient import TestClient
from dayone.common.config import Settings
from dayone.common.store import MemoryStore
from dayone.app.server import create_app

class FakeLauncher:
    def __init__(self): self.launched = []
    def launch(self, run_id, repo_url): self.launched.append((run_id, repo_url))

def mk():
    store, launcher = MemoryStore(), FakeLauncher()
    app = create_app(store=store, launcher=launcher,
                     settings=Settings(environ={"DAYONE_SCHED_TOKEN": "tok"}))
    return TestClient(app), store, launcher

def test_trigger_creates_run_and_launches():
    c, store, launcher = mk()
    r = c.post("/runs", json={})
    assert r.status_code == 200
    rid = r.json()["run_id"]
    assert store.get_run(rid).status == "queued"
    assert launcher.launched and launcher.launched[0][0] == rid

def test_cooldown_429():
    c, *_ = mk()
    assert c.post("/runs", json={}).status_code == 200
    assert c.post("/runs", json={}).status_code == 429

def test_events_after():
    c, store, _ = mk()
    rid = c.post("/runs", json={}).json()["run_id"]
    store.append_event(rid, "plan", {}); store.append_event(rid, "exec", {})
    evs = c.get(f"/api/runs/{rid}/events?after=0").json()
    assert len(evs) == 1 and evs[0]["type"] == "exec"

def test_scheduled_requires_token():
    c, _, launcher = mk()
    assert c.post("/internal/scheduled?token=bad").status_code == 403
    assert c.post("/internal/scheduled?token=tok").status_code == 200
    assert len(launcher.launched) == 2
```

Run: `uv run pytest tests/test_server.py -v` → Expected: FAIL

- [ ] **Step 2: server.py / launcher.py 実装（SSE は `StreamingResponse` + generator。本番組み立て（`create_app()` 引数なし）のときだけ `FirestoreStore` と `CloudRunJobLauncher` を生成。`RunJobRequest` の env 上書きは `overrides={"container_overrides": [{"env": [{"name": "DAYONE_RUN_ID", "value": run_id}, {"name": "DAYONE_REPO_URL", "value": repo_url}]}]}`）**

- [ ] **Step 3: テスト通過確認** — `uv run pytest tests/ -v` → 全 PASS
- [ ] **Step 4: デプロイして本番トリガー確認**

```bash
docker buildx build --platform linux/amd64 -f Dockerfile.app -t "$AR/app:dev2" --push .
gcloud run deploy dayone-app --image "$AR/app:dev2" --region asia-northeast1 \
  --update-env-vars "DAYONE_SCHED_TOKEN=$(openssl rand -hex 16)"
curl -X POST "$APP_URL/runs" -H 'content-type: application/json' -d '{}'
```

Expected: `{"run_id": "…"}` が返り、Cloud Run Jobs の実行が始まり、`GET /api/runs/{id}/events` が増えていく

- [ ] **Step 5: コミット** — `git commit -am "feat: app API with trigger, cooldown, SSE"`

---

### Task 12: ダッシュボード UI

**Files:**
- Create: `src/dayone/app/static/index.html`（+ `server.py` の `/` を FileResponse に変更）
- Test: 目視（自動テストなし）。`tests/test_server.py` に `/` が 200 で text/html を返すテストだけ追加

**Interfaces:**
- Consumes: `GET /api/runs`, `GET /api/runs/{id}/events`, SSE `/api/runs/{id}/stream`, `POST /runs`

**画面仕様（1ファイル・ビルドレス・フレームワークなし）:**
- ヘッダ: ロゴ「DayOne ☀️」+ キャッチ「毎日が入社初日。」+「今日のルーキーを入社させる」ボタン（POST /runs、429 なら「クールダウン中」トースト）
- 左カラム: 実行履歴リスト（時刻・リポジトリ名・status バッジ・腐敗スコア）+ 腐敗スコア推移の折れ線（インライン SVG、直近10run）
- 右カラム: 選択中 run のタイムライン。イベント type ごとにアイコン（plan 📋 / exec ▶ / diagnose 🔍 / fix 🔧 / report 📝 / error ⚠）、`think` は吹き出しスタイル。running 中は SSE 購読で自動追記、`done` で status 再取得
- 摩擦レポートカード: frictions テーブル（step / 分類 / 深刻度 / 説明）+ decay_score の大きな数字 + TTFS + PR リンクボタン
- スタイル: システムフォント、ダーク基調（#0f172a 背景 / #38bdf8 アクセント）、CSS 変数でまとめる。スマホ対応は不要

- [ ] **Step 1: `/` の 200 テスト追加 → FAIL 確認**
- [ ] **Step 2: index.html 実装（fetch + EventSource の素の JS、~250 行以内）**
- [ ] **Step 3: ローカル起動して目視確認** — `DAYONE_STORE=memory uv run dayone-app` + ブラウザで `http://localhost:8080`（MemoryStore ではデータが無いので、`POST /runs` の代わりにテスト用 seed エンドポイントは作らず、Task 11 デプロイ済み本番 URL で確認してよい）
- [ ] **Step 4: デプロイ + コミット** — イメージ再ビルド→deploy、`git commit -am "feat: dashboard UI"`

---

### Task 13: Cloud Scheduler + CI/CD（GitHub Actions, WIF）+ 毎日 E2E

**Files:**
- Create: `.github/workflows/deploy.yml`, `.github/workflows/e2e.yml`, `scripts/setup_wif.sh`, `scripts/e2e_check.py`
- Test: push して Actions が緑になること

**Interfaces:**
- Consumes: Task 1-12 の成果物一式

- [ ] **Step 1: Cloud Scheduler ジョブ作成（毎朝 07:00 JST の定期入社）**

```bash
APP_URL=$(gcloud run services describe dayone-app --region asia-northeast1 --format='value(status.url)')
TOKEN=<Task11 で設定した DAYONE_SCHED_TOKEN>
gcloud scheduler jobs create http dayone-morning --location asia-northeast1 \
  --schedule "0 7 * * *" --time-zone "Asia/Tokyo" \
  --uri "$APP_URL/internal/scheduled?token=$TOKEN" --http-method POST
gcloud scheduler jobs run dayone-morning --location asia-northeast1   # 手動発火で検証
```

- [ ] **Step 2: WIF セットアップ（scripts/setup_wif.sh）**

```bash
#!/usr/bin/env bash
set -euo pipefail
PROJECT_ID=$(gcloud config get-value project)
PROJECT_NUM=$(gcloud projects describe "$PROJECT_ID" --format='value(projectNumber)')
REPO="1729kent/dayone"
gcloud iam workload-identity-pools create github --location global 2>/dev/null || true
gcloud iam workload-identity-pools providers create-oidc github-oidc \
  --location global --workload-identity-pool github \
  --issuer-uri "https://token.actions.githubusercontent.com" \
  --attribute-mapping "google.subject=assertion.sub,attribute.repository=assertion.repository" \
  --attribute-condition "assertion.repository=='$REPO'" 2>/dev/null || true
gcloud iam service-accounts create dayone-deployer 2>/dev/null || true
for role in roles/run.admin roles/artifactregistry.writer roles/iam.serviceAccountUser; do
  gcloud projects add-iam-policy-binding "$PROJECT_ID" \
    --member "serviceAccount:dayone-deployer@${PROJECT_ID}.iam.gserviceaccount.com" --role "$role" -q --condition=None
done
gcloud iam service-accounts add-iam-policy-binding "dayone-deployer@${PROJECT_ID}.iam.gserviceaccount.com" \
  --role roles/iam.workloadIdentityUser \
  --member "principalSet://iam.googleapis.com/projects/${PROJECT_NUM}/locations/global/workloadIdentityPools/github/attribute.repository/${REPO}"
echo "provider: projects/${PROJECT_NUM}/locations/global/workloadIdentityPools/github/providers/github-oidc"
```

- [ ] **Step 3: deploy.yml（push→test→build→deploy）**

```yaml
name: deploy
on: { push: { branches: [main] } }
permissions: { contents: read, id-token: write }
env:
  REGION: asia-northeast1
  PROJECT_ID: <PROJECT_ID>   # 実値を記入
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync && uv run ruff check . && uv run pytest -q
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: <setup_wif.sh の出力>
          service_account: dayone-deployer@<PROJECT_ID>.iam.gserviceaccount.com
      - uses: google-github-actions/setup-gcloud@v2
      - run: gcloud auth configure-docker $REGION-docker.pkg.dev -q
      - run: |
          AR="$REGION-docker.pkg.dev/$PROJECT_ID/dayone"
          docker build -f Dockerfile.app -t "$AR/app:$GITHUB_SHA" . && docker push "$AR/app:$GITHUB_SHA"
          docker build -f Dockerfile.rookie -t "$AR/rookie:$GITHUB_SHA" . && docker push "$AR/rookie:$GITHUB_SHA"
          gcloud run deploy dayone-app --image "$AR/app:$GITHUB_SHA" --region $REGION
          gcloud run jobs update dayone-rookie --image "$AR/rookie:$GITHUB_SHA" --region $REGION
```

- [ ] **Step 4: e2e.yml（毎日 06:00 JST に回帰・エージェント品質の継続検証）**

`scripts/e2e_check.py`: `POST $APP_URL/runs` → 60 秒ごとに `GET /api/runs/{id}` をポーリング（最大 12 分）→ `status == "completed"` かつ `decay_score > 0`（腐敗を検知できている）を assert、失敗で exit 1。e2e.yml は `schedule: cron "0 21 * * *"`（UTC）+ `workflow_dispatch`、ubuntu-latest で `uv run python scripts/e2e_check.py` を実行、`APP_URL` は repo variable。

- [ ] **Step 5: push して Actions 緑確認 → コミット済みの状態にする**

---

### Task 14: README・アーキ図・提出素材（+ 余裕があれば ADK ラッパ）

**Files:**
- Create: `README.md`（日本語・提出向け）, `docs/architecture.png`（draw.io か Mermaid→PNG）
- Optional: `src/dayone/adk_app/`（ADK SequentialAgent ラッパ + `adk eval` ケース）

**README 必須構成（ProtoPedia ストーリー3項目と審査基準5軸に対応させる）:**
1. ヒーローセクション: 1 行コンセプト + デモ URL + スクショ
2. 解決したい課題とその背景（ドキュメント腐敗・新人の半日）
3. 想定ユーザー / プロダクトの特徴（自律ループの説明図）
4. アーキテクチャ図 + 技術スタック（Cloud Run / Jobs / Scheduler / Firestore / Vertex AI Gemini）
5. エージェントの自律性: 計画→実行→診断→修復→検証→報告のループ、HITL 設計
6. セキュリティ設計（最小権限 SA・環境変数スクラブ・予算/タイムアウト）
7. 定量効果: 腐敗スコア・TTFS のグラフ（実測スクショ）
8. DevOps 実践: CI/CD（WIF）、毎日の E2E 回帰、Cloud Scheduler 常駐運用
9. セットアップ手順（自分の README こそ腐らせない: e2e が毎日検証していることを明記）

**Optional ADK ラッパ（時間が余ったときのみ・go/no-go は 7/8 昼判断):** adk.dev の最新ドキュメントを取得し、Planner/Diagnostician/Reporter を ADK の `LlmAgent`+`FunctionTool` に写経して `adk web` でトレース可視化できるようにする。うまく動かなければ即撤退（コアは genai SDK 版で完成済みなので提出物に影響なし）。README には使用技術として正直に書く。

- [ ] **Step 1: README.md 執筆（上記構成・スクショは Task 12 の本番 UI から取得）**
- [ ] **Step 2: Mermaid でアーキ図を書き PNG 化（ProtoPedia の「システム構成」必須項目用）**
- [ ] **Step 3: 最終コミット + タグ** — `git tag v1.0.0 && git push --tags`

---

## Self-Review 結果（プラン作成時に実施済み）

1. **Spec coverage**: 設計書 §3(構成)=Task 1/10/11、§4.1(エージェント)=Task 5-8/10、§4.2(app)=Task 11/12、§4.3(データ)=Task 2、§4.4(デモリポ)=Task 9、§5(セキュリティ)=Task 3(スクラブ)/Task 1(SA)/Task 9(PAT)、§6(エラー処理)=Task 4(リトライ)/Task 6(予算)/Task 10(部分レポート)、§7(テスト)=各タスク+Task 13(毎日E2E)、§8(CI/CD)=Task 13、§9(提出物)=Task 14。**ギャップ**: 設計書の「ADK SequentialAgent をルート」は実装リスク（2.0 の破壊的変更・オフライン検証不可）のため genai SDK 直実装を主、ADK を Task 14 の Optional に降格 — 要ユーザー同意（提出要件は Gemini+Cloud Run で充足）。
2. **Placeholder scan**: `<PROJECT_ID>` / `<PAT>` / `<setup_wif.sh の出力>` は実行時に確定する実値の差し込み位置として意図的に残置。コード上の TODO なし。
3. **Type consistency**: `StepOutcome.suspicious`（Task 2 定義・Task 6 使用）、`Fix.working_command`（Task 2 定義・Task 6/7/8 使用）、`Store.append_event -> int`（Task 2 定義・Task 10/11 使用）、`EmitFn`（Task 6 定義・Task 8/10 消費）を突合済み。

